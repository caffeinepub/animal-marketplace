{
  "frontend/src/index.css": "Global styles with Professional Blue OKLCH color system, increased default border-radius to 0.75rem for modern rounded corners, global rounded corner rules for inputs/buttons, solid white background enforcement, and solid white dropdown/popover overrides.",
  "frontend/tailwind.config.js": "Extended Tailwind config updated with Plus Jakarta Sans display font, Professional Blue color palette tokens, and blue-toned card shadow utilities replacing the warm amber shadows.",
  "frontend/src/App.tsx": "Root app with TanStack Router; CattleTrackerRouteGuard reads principal directly from identity (no async call), shows a \"Verifying access...\" loading spinner with solid white background while auth resolves, and all loading screens use solid white backgrounds.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds the missing `useGetListing` (single listing by id), `useUpdateListing` (update listing fields), `useIsManagementUser`/`useIsOwnerUser`/`useIsCattleTrackerUser` stubs, and `useIsAdmin` returning both `isAdmin` boolean and `isLoading` state; all mutations use `waitForActor` retry logic.",
  "frontend/src/components/Layout.tsx": "App shell with solid Professional Blue header, round logo, version badge updated to `v55`, BottomNav, and solid white footer with Version 55 label and caffeine.ai attribution; decorative header elements have `pointer-events: auto` to ensure interactivity.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login modal dialog prompting new users to enter a display name and optional bio; updated to pass explicit `contactInfo: null` and `mobileNumber: null` to `useSaveCallerUserProfile` to satisfy the full parameter type.",
  "frontend/src/components/ListingCard.tsx": "Reusable listing card with rounded-2xl corners on the card and image frame, solid white card background, VIP gold border/badge, and category badge; all sharp corners replaced with rounded-xl/2xl variants.",
  "frontend/src/components/PhotoGallery.tsx": "Photo carousel with rounded-2xl main image frame, rounded-xl thumbnails, solid white navigation button backgrounds, and photo counter; all sharp corners replaced with modern rounded variants.",
  "frontend/src/components/ChatThread.tsx": "Real-time chat UI with message history, date separators, auto-scroll, Quick Reply buttons, and a send button; fixes type errors by converting the `recipientPrincipal` string to a `Principal` object via `Principal.fromText()` before passing it to `useGetConversation`, `useGetProfile`, and `useSendMessage`.",
  "frontend/src/pages/HomePage.tsx": "Browse page with hero banner, search/filter bar, featured VIP listings section, and main listings grid; the hero overlay gradient div now has `pointer-events: none` so it cannot intercept clicks on the bottom navigation or other interactive elements.",
  "frontend/src/pages/ListingDetailPage.tsx": "Full listing detail view with photo gallery, metadata, seller profile card, and Contact Seller button; fixes the navigation error by removing the non-existent `/messages/$principal` route and navigating to `/messages` instead.",
  "frontend/src/pages/MessagesPage.tsx": "Two-panel messages page with conversation list sidebar and chat thread view; fixes the type error in `ConversationItem` by converting the `principalStr` string to a `Principal` object via `Principal.fromText()` before passing it to `useGetProfile`.",
  "frontend/src/pages/PostAdPage.tsx": "5-step ad posting wizard (Details → Photos → Price → Location → Review) using data URLs for photo storage (no external blob dependency); fixes `onSelect` prop name for `LocationPickerModal` and passes required `isSubmitting` prop to `PaymentModal`.",
  "frontend/src/pages/ProfilePage.tsx": "OLX-style Account page with rounded-xl/2xl dialog modals using solid white backgrounds, rounded-xl buttons and inputs throughout, version label updated to Version 54, and all sharp corners replaced.",
  "frontend/src/lib/utils.ts": "Utility function `cn` combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/SignUpPage.tsx": "OLX-style login screen with the Animal Pashu Bazar round logo at top, three vertically stacked login buttons (Continue with Phone triggers Internet Identity, Continue with Google/Facebook show coming-soon toasts), a Terms & Conditions checkbox, and a second step phone-registration form shown after successful login.",
  "frontend/index.html": "HTML entry point with favicon referencing the app-logo asset, PWA meta tags, and theme-color set to Professional Blue.",
  "frontend/src/components/PaymentModal.tsx": "Payment modal with rounded-2xl container and solid white background, rounded-xl inputs/buttons, solid white promo feedback panels, and all transparency removed from backgrounds.",
  "frontend/src/pages/HelplinePage.tsx": "Help & Support page with a prominent 'Call Admin for Ad Approval' button (tel:8431207976) in the Contact Details section, OLX-style menu rows, and a contact form.",
  "frontend/src/pages/TermsPage.tsx": "Terms & Conditions page with all brand references updated to \"Animal Pashu Bazar\".",
  "frontend/src/pages/AdminDashboardPage.tsx": "Tabbed Admin Dashboard with five tabs — App Statistics (now includes \"Total Ads Posted\" and \"Total Successful Logins\" stat cards), Pending Listings, All Listings Management, Registered Users (mobile numbers), and a new \"User Activity\" tab showing a table of every user's principal, display name, mobile number (cross-referenced from `getAllMobileNumbers`), and last login date formatted as a human-readable string.",
  "frontend/src/hooks/useWebShare.ts": "Custom hook that wraps the Web Share API with a clipboard fallback; invokes `navigator.share` when available, otherwise copies the full share text+URL to the clipboard and shows a sonner toast notification.",
  "frontend/src/main.tsx": "App entry point that registers the Service Worker on load, wraps the app with `ThemeProvider`, `InternetIdentityProvider` (with `disableIdle: true` and a 7-day `maxTimeToLive`), and `QueryClientProvider`.",
  "frontend/public/sw.js": "Service Worker with network-first navigation (app-shell fallback), cache-first static assets, and explicit bypass rules for all Internet Identity / ICP origins (`identity.ic0.app`, `identity.internetcomputer.org`, `ic0.app`, `icp0.io`, etc.) and `/api/v2/` + `/api/v3/` canister API paths so auth flows are never intercepted.",
  "frontend/public/manifest.json": "PWA manifest with Animal Pashu Bazar branding, Professional Blue theme color, and icons referencing the generated app-logo assets.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor using the current Internet Identity; uses a singleton `AuthClient` created with `disableIdle: true` and `disableDefaultIdleCallback: true` for persistent PWA sessions, and re-initializes the actor whenever the identity or initialization state changes.",
  "frontend/src/pages/UserDashboardPage.tsx": "User dashboard page showing profile editing and My Ads grid with deactivate functionality; imports `useUpdateListing` which is now properly exported from `useQueries.ts`.",
  "frontend/src/pages/ManagementPage.tsx": "Read-only Management Dashboard page at `/management` showing a \"Registered Users\" table (principal + phone number via `useGetAllMobileNumbers`) and an \"All Listings\" card grid (title, category, location, price in ₹, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`), styled with the Professional Blue theme.",
  "frontend/src/pages/OwnerViewPage.tsx": "New Owner View page at `/owner-view` displaying two sections: a \"Registered Users\" table (principal + mobile number via `useGetAllMobileNumbers`) and an \"All Animal Ads\" card list (title, category, location, price, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`) with a \"Mark as Live\" button per non-approved listing that calls `useApproveListing` and refreshes the list on success.",
  "frontend/src/pages/CattleTrackerPage.tsx": "Private Cattle Tracker page at `/tracker` displaying two sections — a \"Registered Users\" table (principal + mobile number via `useGetAllMobileNumbers`) and an \"All Animal Posts\" card grid (title, category, location, price in ₹, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`), styled consistently with the Professional Blue theme.",
  "frontend/src/components/BottomNav.tsx": "Fixed bottom navigation bar with `z-index: 999` on the root `<nav>` and all tab buttons; the Sell button's decorative inner divs have `pointer-events: none` so only the parent button receives clicks; all five tabs (Home, Chats, Sell, My Ads, Account) have explicit `pointer-events: auto` and `position: relative`.",
  "frontend/src/components/LocationPickerModal.tsx": "Full-screen OLX-style location picker modal with solid white backgrounds throughout (no transparency), rounded-xl search input, and all rows/buttons using solid white backgrounds to prevent text bleed-through."
}