{
  "frontend/src/index.css": "Global styles with Professional Blue OKLCH color system replacing the warm amber/green theme; uses Plus Jakarta Sans for headings and Inter for body text, with white backgrounds and high-contrast dark text.",
  "frontend/tailwind.config.js": "Extended Tailwind config updated with Plus Jakarta Sans display font, Professional Blue color palette tokens, and blue-toned card shadow utilities replacing the warm amber shadows.",
  "frontend/src/App.tsx": "Root app component with TanStack Router; fixes `CattleTrackerRouteGuard`, `ManagementRouteGuard`, and `OwnerViewRouteGuard` to treat both `'initializing'` and `'logging-in'` as loading states, preventing premature redirects before the stored identity is restored from local storage on page load.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds the missing `useGetMobileNumber` export that wraps the backend's `getMobileNumber` method (used by `PostAdPage` to check if the user has completed sign-up), and also invalidates the `mobileNumber` query key on successful sign-up.",
  "frontend/src/components/Layout.tsx": "App shell with Professional Blue header, privileged-only desktop nav, BottomNav, footer, and main content area; version label updated from \"Version 48\" to \"Version 49\".",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login modal dialog prompting new users to enter a display name and optional bio; updated to pass explicit `contactInfo: null` and `mobileNumber: null` to `useSaveCallerUserProfile` to satisfy the full parameter type.",
  "frontend/src/components/ListingCard.tsx": "Reusable listing card that now renders a gold/amber border and a \"Verified\" badge (with ShieldCheck icon) for VIP listings, and a gold accent bar below the image; non-VIP cards retain their original styling.",
  "frontend/src/components/PhotoGallery.tsx": "Photo carousel with main image, prev/next navigation, thumbnail strip, error fallbacks, and photo counter; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/ChatThread.tsx": "Real-time chat UI with message history, date separators, auto-scroll, Quick Reply buttons, and a send button; fixes type errors by converting the `recipientPrincipal` string to a `Principal` object via `Principal.fromText()` before passing it to `useGetConversation`, `useGetProfile`, and `useSendMessage`.",
  "frontend/src/pages/HomePage.tsx": "Browse page with hero banner, search/filter bar, Featured Animals VIP section, and listings grid; the \"Invite a friend\" banner section has been removed entirely, and unused imports (useWebShare, Share2, Users) have been cleaned up.",
  "frontend/src/pages/ListingDetailPage.tsx": "Full listing detail view with photo gallery, metadata, seller profile card, and Contact Seller button; fixes the type error by converting the owner principal string to a `Principal` object via `Principal.fromText()` before passing it to `useGetProfile`.",
  "frontend/src/pages/MessagesPage.tsx": "Two-panel messages page with conversation list sidebar and chat thread view; fixes the type error in `ConversationItem` by converting the `principalStr` string to a `Principal` object via `Principal.fromText()` before passing it to `useGetProfile`.",
  "frontend/src/pages/PostAdPage.tsx": "Post Ad form page with VIP option, photo upload, and payment modal; updated `flex-shrink-0` to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/pages/ProfilePage.tsx": "Authenticated profile page showing user info with inline edit form, active listings grid, and deactivate listing functionality; updated `handleSaveProfile` to pass `contactInfo: null` and `mobileNumber: null` to `useSaveCallerUserProfile`.",
  "frontend/src/lib/utils.ts": "Utility function `cn` combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/SignUpPage.tsx": "Sign-up page with fixed actor readiness check — shows a \"Connecting…\" spinner in the button and a network notice banner while the actor is initializing (actorFetching), disables the submit button until the actor is ready, and displays a friendly inline error instead of the raw \"Actor not available\" message.",
  "frontend/index.html": "HTML entry point with browser tab title updated to \"Animal Pashu Bazar\" and favicon referencing the app-logo asset.",
  "frontend/src/components/PaymentModal.tsx": "Payment modal supporting both regular UPI payment flow and a special admin promo code (`FREEADMIN`) that sets the price to ₹0, hides the QR/UPI payment UI, and shows a green \"Publish My Ad — Free\" button that calls `onConfirmPayment` directly without going through the payment confirmation step; the existing `PASHU50` and other promo codes continue to work as before for non-VIP ads.",
  "frontend/src/pages/HelplinePage.tsx": "Customer Helpline page with a contact form and a Contact Details section showing the updated support email (irfankhansingboard1998@gmail.com) and two clickable helpline numbers (7829297025 and 8431207976) with the 10 AM–6 PM availability note preserved.",
  "frontend/src/pages/TermsPage.tsx": "Terms & Conditions page with all brand references updated to \"Animal Pashu Bazar\".",
  "frontend/src/pages/AdminDashboardPage.tsx": "Tabbed Admin Dashboard with five tabs — App Statistics (now includes \"Total Ads Posted\" and \"Total Successful Logins\" stat cards), Pending Listings, All Listings Management, Registered Users (mobile numbers), and a new \"User Activity\" tab showing a table of every user's principal, display name, mobile number (cross-referenced from `getAllMobileNumbers`), and last login date formatted as a human-readable string.",
  "frontend/src/hooks/useWebShare.ts": "Custom hook that wraps the Web Share API with a clipboard fallback; invokes `navigator.share` when available, otherwise copies the full share text+URL to the clipboard and shows a sonner toast notification.",
  "frontend/src/main.tsx": "App entry point that registers the Service Worker on load, wraps the app with `ThemeProvider`, `InternetIdentityProvider` (with `disableIdle: true` and a 7-day `maxTimeToLive`), and `QueryClientProvider`.",
  "frontend/public/sw.js": "Service Worker with network-first navigation (app-shell fallback), cache-first static assets, and explicit bypass rules for all Internet Identity / ICP origins (`identity.ic0.app`, `identity.internetcomputer.org`, `ic0.app`, `icp0.io`, etc.) and `/api/v2/` + `/api/v3/` canister API paths so auth flows are never intercepted.",
  "frontend/public/manifest.json": "PWA manifest with name and short_name updated to \"Animal Pashu Bazar\" and description updated accordingly.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates the backend actor using the current Internet Identity; uses a singleton `AuthClient` created with `disableIdle: true` and `disableDefaultIdleCallback: true` for persistent PWA sessions, and re-initializes the actor whenever the identity or initialization state changes.",
  "frontend/src/pages/UserDashboardPage.tsx": "New user dashboard page at `/dashboard` showing a personalized welcome heading, an editable \"My Profile\" section (display name + bio), and a \"My Ads\" grid of the user's active listings with deactivate functionality. Contains no admin-only UI elements and is styled consistently with the Professional Blue theme.",
  "frontend/src/pages/ManagementPage.tsx": "Read-only Management Dashboard page at `/management` showing a \"Registered Users\" table (principal + phone number via `useGetAllMobileNumbers`) and an \"All Listings\" card grid (title, category, location, price in ₹, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`), styled with the Professional Blue theme.",
  "frontend/src/pages/OwnerViewPage.tsx": "New Owner View page at `/owner-view` displaying two sections: a \"Registered Users\" table (principal + mobile number via `useGetAllMobileNumbers`) and an \"All Animal Ads\" card list (title, category, location, price, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`) with a \"Mark as Live\" button per non-approved listing that calls `useApproveListing` and refreshes the list on success.",
  "frontend/src/pages/CattleTrackerPage.tsx": "Private Cattle Tracker page at `/tracker` displaying two sections — a \"Registered Users\" table (principal + mobile number via `useGetAllMobileNumbers`) and an \"All Animal Posts\" card grid (title, category, location, price in ₹, status badge, seller principal, photo thumbnail via `useGetAllListingsAdmin`), styled consistently with the Professional Blue theme.",
  "frontend/src/components/BottomNav.tsx": "Fixed bottom navigation bar with five tabs (Home, Chats, Sell, My Ads, Share); the former \"Account\" tab is replaced with a \"Share\" tab that triggers the Web Share API via the useWebShare hook, the Sell tab remains a prominent raised circular button, and active tabs are highlighted in primary blue."
}